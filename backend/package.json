{
  "name": "consentflow-adapter",
  "version": "1.0.0",
  "description": "ConsentFlow Cleanverse adapter backend — wraps Cleanverse sandbox API (CVI A-Pass, CVA A-Token rules, CCP verify_apass) behind a local REST API.",
  "main": "index.js",
  "type": "commonjs",
  "scripts": {
    "start": "node index.js",
    "test": "node test-adapter.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "ethers": "^6.17.0",
    "express": "^4.19.2",
    "express-rate-limit": "^8.6.2"
  }
}
