[profile.default]
src = "contracts"
test = "test"
out = "out"
libs = ["lib"]
evm_version = "prague"
solc_version = "0.8.24"
optimizer = true
optimizer_runs = 200

remappings = [
    "@openzeppelin/=lib/openzeppelin-contracts/"
]
